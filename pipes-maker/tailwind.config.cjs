const config = {
  content: ['./src/**/*.{html,js,svelte,ts}'],

  theme: {
    extend: {
      fontFamily: {
        mono: ['Latin Modern Mono', 'monospace'],
      },
    },
  },

  plugins: [],
};

module.exports = config;
